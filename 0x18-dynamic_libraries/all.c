#include <stdio.h>
#include <stdlib.h>
/**
 * add - addition of two numbers
 * @a: the value of A
 * @b: the value of b
 * return: Addition
 */
int add(int a, int b)
{
	return(a + b);
}

/**
 * sub - subtrion of two numbers
 * @a: the value of A
 * @b: the value of b
 * return: subtraction
 */
int sub(int a, int b)
{
	return(a - b);
}

/**
 * mul - mul of two numbers
 * @a: the value of A
 * @b: the value of b
 * return: Addition
 */
int mul(int a, int b)
{
        return(a * b);
}

/**
 * div - dev of two numbers
 * @a: the value of A
 * @b: the value of b
 * return: divishon
 */
int div(int a, int b)
{
        return(a / b);
}

/**
 * moud - modulation of two numbers
 * @a: the value of A
 * @b: the value of b
 * return: modulation
 */
int moud(int a, int b)
{
        return(a % b);
}
