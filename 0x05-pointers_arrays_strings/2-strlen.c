#include <stdio.h>
/**
 * _strlen - to return chake the lengeth of the strlen
 * @s : the value of sterlen
 * Return: leangth
 */
int _strlen(char *s)
{
s_t lengeth = 0;
while (*s++)
length++;
return (length);
}
