#include "main.h"
/**
 * mu - alphabet in lowercase
 * @c: the character in ASCII code
 * Return: 1 for lowaer character, 0 for the rest
 */
int mul(int a, int b);
{
int mul = 0;
num = (a * b);
putchar(num + '0');
}
putchar('\n');
return (0);
}
