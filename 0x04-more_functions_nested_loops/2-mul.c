#include "main.h"
/**
 * mul - alphabet in lowercase
 * @a: the first num to multiplay
 * @b: the second number
 * Return: 1 for lowaer character, 0 for the rest
 */
int mul(int a, int b);
{
return (a * b);
}
